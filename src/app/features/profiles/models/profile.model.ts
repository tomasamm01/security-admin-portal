export interface Profile {
  idProfile         ?: number;
  nombrePerfil            : string;
  descripcion       ?: string;
  activo            : boolean;
  userLog           ?: string;
  fechaLog          ?: Date;
  fechaAlta         ?: Date;
}
